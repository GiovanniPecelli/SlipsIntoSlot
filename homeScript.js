/* Freccia per tornare al top dalla pagina */

/* const backBtn = document.getElementById('backBtn');

backBtn.addEventListener('click', function() {
    window.scrollTo(0, 0); iporta la pagina all'inizio con un click 
});

window.addEventListener('scroll', function() {
    let posY = window.scrollY;

    if(posY > 0) {
        backBtn.style.display = 'block';  cosi faccio tornare visibile l'elemento nascosto 
    } else {  prima devo nascoderlo sul css 
        backBtn.style.display = 'none';
    }
}); */